import { ArchiveNode } from './types';

const DIV = "\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n\n";

export const knowledgeNodes: ArchiveNode[] = [
    {
        path: "after-hours://knowledge/html",
        title: "HTML",
        category: "knowledge",
        status: "FILED",
        version: "5",
        filed: "2026-05-26",
        role: "Structural markup language for web documents.",
        language: "HTML",
        environment: "Browser",
        installedInProject: true,
        dependencies: [],
        tags: ["markup", "structure", "web", "dom"],
        content: {
            en: "FUNCTION\n\nHTML (HyperText Markup Language) is the standard structural language for web documents. It defines the document tree — elements, their nesting relationships, and their semantic roles. The browser parses HTML source into the DOM (Document Object Model), a live in-memory tree that JavaScript and CSS then read and modify at runtime. HTML is not a programming language; it carries no execution logic. Its role is to declare the structure and meaning of content." + DIV + "CORE CONCEPTS\n\nElements are defined by opening and closing tags. An element can carry attributes that modify its behavior: id assigns a unique identifier, class assigns one or more style hooks, href specifies a link destination, src specifies a resource location. Attributes are parsed into the DOM node's property surface.\n\nHTML5 introduced semantic elements — header, main, section, article, nav, aside, footer — that communicate document structure to both browsers and assistive technologies. These replace the generic div-based layouts common in earlier HTML versions.\n\nThe document is divided into two major sections. The head carries metadata, stylesheet links, and script references that are not directly rendered. The body carries all visible content. The browser builds two parallel trees from the parsed document: the DOM (used by JavaScript) and the accessibility tree (used by screen readers and assistive tools)." + DIV + "INTEGRATION CONTEXT\n\nHTML is the lowest rendered layer in every browser-based web application. Frontend frameworks like React and Vue do not write HTML directly — they operate through abstraction layers that eventually produce DOM mutations. React uses JSX syntax that compiles to React.createElement calls, which the virtual DOM system reconciles against the real browser DOM. The HTML document itself (index.html) acts as the mount point for the React root.\n\nThis workstation uses HTML as the document container for the React OS shell (workspace-os/public/index.html) and for the 3D room entry point (workspace-3d/index.html). The OS interface is embedded into the Three.js 3D scene via an iframe, which loads the HTML document of the OS workspace inside the browser's compositor." + DIV + "TRADEOFFS\n\nHTML alone carries no interactivity or visual presentation. It requires CSS for layout and typography and JavaScript for behavior. This separation of concerns is intentional and foundational — it allows each layer to evolve independently and to be applied or overridden selectively.\n\nThe strict parsing model of HTML5 makes it more forgiving than XML, but malformed markup can produce unexpected DOM structures that cause layout or scripting errors. Semantic elements improve accessibility and search engine indexing, but their correct use requires discipline — a div used as a button does not carry keyboard focus or ARIA roles automatically.",
            id: "FUNGSI\n\nHTML (HyperText Markup Language) adalah bahasa markup standar untuk dokumen web. HTML mendefinisikan pohon dokumen — elemen, hubungan nesting-nya, dan peran semantiknya. Browser mengurai HTML untuk membangun DOM." + DIV + "KONSEP INTI\n\nElemen didefinisikan oleh tag. Elemen membawa atribut (id, class, href, src). HTML5 memperkenalkan elemen semantik seperti header, main, section, article, dan nav yang menyampaikan makna di luar tata letak. Dokumen dibagi menjadi head (metadata) dan body (konten yang dapat dilihat)." + DIV + "KONTEKS INTEGRASI\n\nHTML adalah lapisan yang dirender paling rendah di setiap aplikasi web berbasis browser. React tidak menulis HTML secara langsung — menghasilkan JSX yang dikompilasi menjadi panggilan React.createElement, yang pada akhirnya direkonsiliasi terhadap DOM browser nyata." + DIV + "TRADEOFF\n\nHTML saja tidak memiliki interaktivitas atau presentasi visual. Membutuhkan CSS untuk tata letak dan JavaScript untuk perilaku. Elemen semantik meningkatkan aksesibilitas tetapi penggunaannya yang benar memerlukan disiplin."
        },
        related: ["after-hours://knowledge/css", "after-hours://knowledge/javascript", "after-hours://knowledge/react"]
    },
    {
        path: "after-hours://knowledge/css",
        title: "CSS",
        category: "knowledge",
        status: "FILED",
        version: "3",
        filed: "2026-05-26",
        role: "Presentation and layout language for web documents.",
        language: "CSS",
        environment: "Browser",
        installedInProject: true,
        dependencies: [],
        tags: ["styling", "layout", "presentation", "web"],
        content: {
            en: "FUNCTION\n\nCSS (Cascading Style Sheets) is the language responsible for the visual presentation of HTML documents. It controls typography, color, spacing, layout, visual states, and motion. CSS rules are applied to HTML elements through selectors. The cascade mechanism determines which rule takes effect when multiple rules target the same element — priority is resolved by specificity, source order, and the presence of the !important declaration." + DIV + "CORE CONCEPTS\n\nSelectors target elements by tag name, class (.classname), ID (#idname), attribute ([type='text']), or structural relationship (descendant, child, sibling combinators). Properties define what aspect to style. Values define the target state.\n\nThe box model governs how elements occupy space. Every element is a rectangular box composed of four concentric regions: content area, padding, border, and margin. The box-sizing property controls whether padding and border are included in the declared width.\n\nFlexbox enables one-dimensional layout (row or column), distributing space among children with alignment controls. CSS Grid enables two-dimensional layout, placing items into explicit rows and columns. Media queries (using @media) allow styles to adapt based on viewport dimensions, device capabilities, or user preferences such as prefers-color-scheme.\n\nCSS custom properties (--variable-name) allow values to be declared once and referenced throughout a stylesheet, functioning as design tokens. Keyframe animations (@keyframes) define multi-step transitions between visual states." + DIV + "INTEGRATION CONTEXT\n\nThis workstation uses two CSS delivery methods in parallel. Most component styling is applied through inline JavaScript objects passed to the React style prop — this approach scopes styles to the component and avoids class name collisions but cannot use pseudo-classes or media queries directly. A global index.css file handles font-face declarations, keyframe animation definitions, scrollbar customization, and class-based hover and active state rules that require pseudo-class selectors.\n\nThe CRT atmosphere classes and scanline overlay effects for the workstation interface are defined in index.css. The window bevel constants (BEVEL object in windowBevel.ts) expose color values that component styles reference directly as string literals." + DIV + "TRADEOFFS\n\nCSS specificity conflicts accumulate in large codebases without a strict naming convention. CSS Modules and CSS-in-JS libraries (styled-components, Emotion) were developed specifically to address scope leakage in component-based architectures.\n\nInline styles in React avoid all specificity conflicts but introduce their own constraints: pseudo-classes (:hover, :focus, :active) and media queries cannot be expressed inline without wrapping logic in JavaScript. The choice between inline styles and class-based styles is an architectural decision that affects maintainability, animation capability, and accessibility behavior.",
            id: "FUNGSI\n\nCSS (Cascading Style Sheets) adalah bahasa yang bertanggung jawab untuk presentasi visual dokumen HTML. CSS mengontrol tipografi, warna, spasi, tata letak, dan keadaan visual. Mekanisme cascade menentukan aturan mana yang berlaku ketika beberapa aturan menargetkan elemen yang sama." + DIV + "KONSEP INTI\n\nSelektor menargetkan elemen berdasarkan nama tag, kelas, ID, atau hubungan struktural. Model kotak mengatur bagaimana elemen menempati ruang: area konten, padding, border, dan margin. Flexbox dan CSS Grid adalah sistem tata letak utama. Media query memungkinkan gaya beradaptasi berdasarkan dimensi viewport." + DIV + "KONTEKS INTEGRASI\n\nWorkstation ini menggunakan dua metode pengiriman CSS secara paralel. Sebagian besar gaya komponen diterapkan melalui objek JavaScript inline yang diteruskan ke prop style React. File index.css global menangani deklarasi font-face, definisi animasi keyframe, dan aturan berbasis kelas yang memerlukan selektor pseudo-class." + DIV + "TRADEOFF\n\nKonflik spesifisitas CSS terakumulasi dalam codebase besar tanpa konvensi penamaan yang ketat. Inline styles di React menghindari semua konflik spesifisitas tetapi tidak dapat menggunakan pseudo-class atau media query tanpa logika JavaScript tambahan."
        },
        related: ["after-hours://knowledge/html", "after-hours://knowledge/javascript", "after-hours://knowledge/react"]
    },
    {
        path: "after-hours://knowledge/javascript",
        title: "JavaScript",
        category: "knowledge",
        status: "FILED",
        version: "ES2022",
        filed: "2026-05-26",
        role: "Primary scripting language of the web platform.",
        language: "JavaScript",
        environment: "Browser / Node.js",
        installedInProject: true,
        dependencies: [],
        tags: ["scripting", "web", "runtime", "ecmascript"],
        content: {
            en: "FUNCTION\n\nJavaScript is the scripting language natively supported by all major web browsers. It enables dynamic behavior in web documents: querying and mutating the DOM, handling user events, performing asynchronous network requests, and managing interface state across user interactions. Node.js extends the same language runtime into server-side environments, allowing JavaScript to run outside the browser for build tools, backend services, and command-line utilities." + DIV + "CORE CONCEPTS\n\nJavaScript is dynamically typed — variable types are determined at runtime rather than declared in advance. The language is single-threaded. All code executes on one thread, coordinated by the event loop, which processes queued tasks and microtasks between rendering frames.\n\nAsynchronous behavior is handled through callbacks, Promises, and the async/await syntax, which allows sequential-looking code to defer execution until a Promise resolves. setTimeout and setInterval schedule deferred and recurring work without blocking the event loop.\n\nJavaScript supports first-class functions: functions are values that can be assigned to variables, passed as arguments, and returned from other functions. Closures allow inner functions to capture and retain access to variables from the scope in which they were defined, even after that outer scope has exited.\n\nES6 (2015) introduced a substantial set of language features: classes, modules (import/export), arrow functions, template literals, destructuring, default parameters, the spread operator, Map, Set, Symbol, and Proxy. Subsequent ECMAScript versions have added optional chaining (?.), nullish coalescing (??), logical assignment operators, and top-level await." + DIV + "INTEGRATION CONTEXT\n\nThis workstation uses TypeScript — a statically typed superset of JavaScript — as its primary authoring language across both workspace-os and workspace-3d. TypeScript is compiled to JavaScript at build time by react-scripts (for the OS) and Webpack (for the 3D environment). At runtime, only JavaScript executes. The Three.js 3D scene, all React component logic, event handlers, and the archive data module are JavaScript at the execution layer." + DIV + "TRADEOFFS\n\nJavaScript's dynamic typing allows flexibility and rapid prototyping, but it makes large codebases harder to maintain without static analysis. Type errors that TypeScript would surface at compile time may only manifest as runtime exceptions in plain JavaScript.\n\nThe single-threaded execution model means long-running synchronous operations block the event loop entirely, causing the interface to freeze. Computationally intensive work must be offloaded to Web Workers or broken into deferred chunks. The module ecosystem (npm) is extensive but introduces dependency management complexity and supply-chain risk.",
            id: "FUNGSI\n\nJavaScript adalah bahasa scripting yang didukung secara native oleh semua browser web utama. Memungkinkan perilaku dinamis dalam dokumen web: manipulasi DOM, penanganan event, komunikasi asinkron, dan manajemen state antarmuka. Node.js memperluas runtime yang sama ke lingkungan sisi server." + DIV + "KONSEP INTI\n\nJavaScript bertipe dinamis dan berjalan pada satu thread, dikoordinasikan oleh event loop. Perilaku asinkron ditangani melalui callback, Promise, dan sintaks async/await. JavaScript mendukung fungsi kelas satu — fungsi adalah nilai yang dapat diteruskan dan dikembalikan. ES6 memperkenalkan kelas, modul, arrow function, destructuring, dan operator spread." + DIV + "KONTEKS INTEGRASI\n\nWorkstation ini menggunakan TypeScript sebagai bahasa authoring utamanya. TypeScript dikompilasi ke JavaScript saat build time. Saat runtime, hanya JavaScript yang dieksekusi — scene Three.js, logika komponen React, dan modul data arsip semuanya adalah JavaScript di lapisan eksekusi." + DIV + "TRADEOFF\n\nPengetikan dinamis JavaScript memungkinkan fleksibilitas tetapi membuat codebase besar lebih sulit dikelola. Model eksekusi single-threaded berarti operasi sinkron yang berjalan lama memblokir event loop sepenuhnya, menyebabkan antarmuka membeku."
        },
        related: ["after-hours://knowledge/typescript", "after-hours://knowledge/html", "after-hours://knowledge/nodejs"]
    },
    {
        path: "after-hours://knowledge/typescript",
        title: "TypeScript",
        category: "knowledge",
        status: "FILED",
        version: "5.x",
        filed: "2026-05-26",
        role: "Statically typed superset of JavaScript.",
        language: "TypeScript",
        environment: "Build-time / Node.js",
        installedInProject: true,
        dependencies: [],
        tags: ["type-safety", "compiler", "javascript", "ecmascript"],
        content: {
            en: "FUNCTION\n\nTypeScript is a statically typed superset of JavaScript developed by Microsoft. It adds optional type annotations, interfaces, enums, generics, and access modifiers to the JavaScript language. TypeScript source is compiled to standard JavaScript before execution — the runtime environment has no awareness of TypeScript types. The compiler erases all type information during the compilation step, producing clean JavaScript output." + DIV + "CORE CONCEPTS\n\nType annotations attach expected types to variables, parameters, and return values. The compiler verifies that actual values match declared types and reports mismatches before the code runs. Primitive types (number, string, boolean, null, undefined, symbol) cover the JavaScript primitives. Literal types (type Direction = 'left' | 'right') restrict values to specific literals. Union types (string | number) allow a value to be one of several types.\n\nInterfaces and type aliases define the shape of objects. Interfaces are extendable through declaration merging; type aliases are not. Generics (function identity<T>(value: T): T) express logic that works across types without losing type information. The compiler infers types where annotations are omitted, reducing boilerplate while retaining safety.\n\nTypeScript uses structural typing — a value satisfies an interface if it has all the required properties, regardless of whether it was explicitly declared as implementing that interface. This contrasts with nominal typing systems used in Java or C#, where explicit type declarations are required." + DIV + "INTEGRATION CONTEXT\n\nTypeScript is installed in both workspace-os and workspace-3d in this project. The React OS layer uses TypeScript for all component prop definitions, hook types, state interfaces, and archive data models. The ArchiveNode interface enforces the shape of every record in the knowledge base at compile time.\n\nThe 3D room (workspace-3d) uses TypeScript for scene management classes, event emitter types, and shader parameter interfaces. The Webpack configuration for workspace-3d uses ts-loader to compile TypeScript before bundling." + DIV + "TRADEOFFS\n\nTypeScript requires a compilation step between authoring and execution. This adds build time and requires compiler configuration (tsconfig.json). Some third-party JavaScript libraries lack TypeScript type definitions — workarounds include installing community-maintained @types packages or declaring module types manually.\n\nTypeScript's type system has limitations. It cannot express certain runtime invariants that the logic relies on. Type assertions (value as Type) allow developers to override compiler inference, which can reintroduce type unsafety if misused. Despite these constraints, the benefit of catching structural mismatches, missing properties, and type coercion errors at compile time is substantial in any codebase maintained over time.",
            id: "FUNGSI\n\nTypeScript adalah superset JavaScript bertipe statis yang dikembangkan oleh Microsoft. Menambahkan anotasi tipe opsional, antarmuka, enum, dan generik ke JavaScript. Kode TypeScript dikompilasi ke JavaScript standar sebelum eksekusi — runtime tidak mengetahui tipe TypeScript." + DIV + "KONSEP INTI\n\nAnotasi tipe melampirkan tipe yang diharapkan ke variabel, parameter, dan nilai kembalian. TypeScript menggunakan structural typing — sebuah nilai memenuhi antarmuka jika memiliki semua properti yang diperlukan. Generik mengekspresikan logika yang bekerja di berbagai tipe tanpa kehilangan informasi tipe." + DIV + "KONTEKS INTEGRASI\n\nTypeScript diinstal di workspace-os dan workspace-3d dalam proyek ini. Lapisan React OS menggunakan TypeScript untuk semua definisi prop komponen, tipe hook, antarmuka state, dan model data arsip. Antarmuka ArchiveNode menerapkan bentuk setiap catatan dalam basis pengetahuan pada waktu kompilasi." + DIV + "TRADEOFF\n\nTypeScript memerlukan langkah kompilasi antara authoring dan eksekusi. Beberapa library JavaScript pihak ketiga tidak memiliki definisi tipe TypeScript. Asersi tipe memungkinkan pengembang mengganti inferensi compiler, yang dapat mengintroduksi kembali type unsafety jika disalahgunakan."
        },
        related: ["after-hours://knowledge/javascript", "after-hours://knowledge/react", "after-hours://knowledge/nodejs"]
    },
    {
        path: "after-hours://knowledge/react",
        title: "React",
        category: "knowledge",
        status: "FILED",
        version: "18.x (general)",
        filed: "2026-05-26",
        role: "Component-based JavaScript library for building user interfaces.",
        language: "JavaScript / TypeScript",
        environment: "Browser",
        installedInProject: true,
        dependencies: ["react-dom"],
        tags: ["interface", "components", "vdom", "hooks"],
        content: {
            en: "FUNCTION\n\nReact is a JavaScript library for building user interfaces through composable, reusable components. A component is a function that accepts data (props) and returns a description of what to display. React maintains a virtual DOM — a lightweight in-memory representation of the current UI state. When state changes, React computes the difference between the previous and current virtual DOM trees (reconciliation) and applies only the necessary mutations to the real browser DOM, minimizing unnecessary layout and repaint operations." + DIV + "CORE CONCEPTS\n\nComponents receive read-only data from parent components through props. Internal mutable state is managed through hooks. useState stores values that trigger re-renders when updated. useEffect registers side effects (subscriptions, DOM measurements, timers, data fetching) that run after each render, with an optional dependency array controlling when the effect re-executes. useRef stores mutable values that persist across renders without causing re-renders.\n\nThe component tree is strictly unidirectional — data flows downward from parent to child. A child component cannot directly modify parent state; it can only invoke callback functions passed down as props. This constraint makes data flow predictable and debuggable.\n\nContext (React.createContext) allows values to be passed through the tree without manual prop threading, useful for theme tokens, locale settings, or shared service instances. React.memo and useMemo provide memoization to prevent unnecessary re-renders in performance-sensitive subtrees." + DIV + "INTEGRATION CONTEXT\n\nThis workstation uses React 17 as its installed interface layer. React 17 predates the concurrent rendering model introduced in React 18 — components render synchronously, and there is no automatic batching of state updates across asynchronous operations. All window components, the desktop shell, the start menu, the taskbar, and the archive browser are React function components written in TypeScript.\n\nThe React application is mounted into the root div of the OS HTML document. The resulting DOM is rendered inside an iframe embedded in the Three.js 3D scene. This iframe boundary separates the React rendering context from the WebGL context." + DIV + "TRADEOFFS\n\nReact handles UI rendering and component state but does not prescribe routing, data fetching, form management, or global state management. Large applications typically compose React with additional libraries: React Router for navigation, React Query or SWR for server state, Zustand or Redux for client state. The need to select and coordinate these additional layers is an architectural decision that affects long-term maintainability.\n\nJSX requires a compilation step (Babel or TypeScript). The virtual DOM reconciliation approach has overhead — for very high-frequency updates (canvas, WebGL, real-time data streams), bypassing React and writing directly to the DOM or canvas API is often more efficient.",
            id: "FUNGSI\n\nReact adalah library JavaScript untuk membangun antarmuka pengguna melalui komponen yang dapat dikomposisikan dan digunakan kembali. React mempertahankan virtual DOM — representasi dalam memori dari keadaan UI saat ini. Ketika state berubah, React menghitung perbedaan antara pohon virtual DOM sebelumnya dan saat ini dan hanya menerapkan mutasi yang diperlukan ke DOM browser nyata." + DIV + "KONSEP INTI\n\nKomponen menerima data hanya-baca dari komponen induk melalui props. State internal yang dapat diubah dikelola melalui hooks: useState, useEffect, useRef. Pohon komponen bersifat unidireksional secara ketat — data mengalir ke bawah dari induk ke anak. Context memungkinkan nilai diteruskan melalui pohon tanpa prop threading manual." + DIV + "KONTEKS INTEGRASI\n\nWorkstation ini menggunakan React 17 sebagai lapisan antarmuka yang diinstal. Semua komponen jendela, shell desktop, start menu, taskbar, dan browser arsip adalah komponen fungsi React yang ditulis dalam TypeScript. Aplikasi React dipasang ke dalam div root dari dokumen HTML OS." + DIV + "TRADEOFF\n\nReact menangani rendering UI dan state komponen tetapi tidak menetapkan routing, pengambilan data, atau manajemen state global. Aplikasi besar biasanya menggabungkan React dengan library tambahan. Pendekatan rekonsiliasi virtual DOM memiliki overhead — untuk pembaruan frekuensi sangat tinggi, menulis langsung ke DOM atau canvas API sering lebih efisien."
        },
        related: ["after-hours://knowledge/javascript", "after-hours://knowledge/typescript", "after-hours://knowledge/vue", "after-hours://knowledge/nextjs"]
    },
    {
        path: "after-hours://knowledge/vue",
        title: "Vue",
        category: "knowledge",
        status: "FILED",
        version: "3.x",
        filed: "2026-05-26",
        role: "Progressive JavaScript framework for building user interfaces.",
        language: "JavaScript / TypeScript",
        environment: "Browser",
        installedInProject: false,
        dependencies: [],
        tags: ["interface", "framework", "reactivity", "components"],
        content: {
            en: "FUNCTION\n\nVue.js is a JavaScript framework for building user interfaces. It organizes interface rendering around declarative components and a fine-grained reactive data system. This record exists as general IT knowledge. This workstation does not use Vue as its installed interface layer — the OS shell is built with React 17." + DIV + "CORE CONCEPTS\n\nVue uses single-file components (.vue files) that co-locate template (HTML), script (JavaScript or TypeScript), and style (CSS) in one file. The template section uses directive syntax to bind data and express conditional or iterative rendering: v-if controls visibility, v-for iterates over arrays, v-bind binds attribute values to reactive data, v-model establishes two-way binding between form inputs and reactive state.\n\nVue 3 introduced the Composition API as an alternative to the original Options API. The Options API organizes component logic by type: data (reactive state), methods (functions), computed (derived values), and watch (side effects). The Composition API organizes logic by concern, grouping related code into composable setup functions analogous in concept to React Hooks.\n\nVue's reactivity system is built on ES6 Proxies. When a reactive object's property is accessed, Vue tracks which components depend on it. When the property changes, Vue re-renders only the component subtrees that depend on it — a finer-grained approach than React's diffing model." + DIV + "INTEGRATION CONTEXT\n\nVue is commonly adopted in projects where gradual integration matters. It can be loaded as a script tag into an existing HTML page without a build pipeline, or used as a full single-page application framework through the Vite or Vue CLI build toolchain. Nuxt.js is Vue's primary full-stack and server-side rendering framework, analogous in role to Next.js for React.\n\nVue competes with React in the browser UI space. Vue's market presence is stronger in certain regions and industries. Both frameworks solve the same core problem: managing reactive state in component trees." + DIV + "TRADEOFFS\n\nVue's template syntax is closer to standard HTML than JSX, which reduces the cognitive overhead for developers familiar with traditional web development. The two-way binding of v-model simplifies form handling compared to React's controlled component pattern.\n\nHowever, Vue's ecosystem is smaller than React's in absolute terms, meaning fewer third-party component libraries and tooling integrations. The Options and Composition APIs coexist in the same codebase, which can lead to inconsistent patterns within teams. The Proxy-based reactivity system has edge cases — deeply nested or dynamically added properties may require explicit reactive wrapping.",
            id: "FUNGSI\n\nVue.js adalah framework JavaScript untuk membangun antarmuka pengguna. Vue mengorganisir rendering antarmuka di sekitar komponen deklaratif dan sistem data reaktif berbutir halus. Catatan ini ada sebagai pengetahuan IT umum. Workstation ini tidak menggunakan Vue sebagai lapisan antarmuka yang diinstal." + DIV + "KONSEP INTI\n\nVue menggunakan komponen file tunggal (.vue) yang menyatukan template, script, dan style dalam satu file. Sintaks template menggunakan direktif: v-if, v-for, v-bind, v-model. Vue 3 memperkenalkan Composition API sebagai alternatif dari Options API asli. Sistem reaktivitas Vue dibangun di atas ES6 Proxies." + DIV + "KONTEKS INTEGRASI\n\nVue umumnya diadopsi dalam proyek di mana integrasi bertahap penting. Nuxt.js adalah framework full-stack dan server-side rendering utama Vue, analog dengan peran Next.js untuk React. Vue bersaing dengan React di ruang UI browser." + DIV + "TRADEOFF\n\nSintaks template Vue lebih dekat ke HTML standar daripada JSX. Sistem reaktivitas berbasis Proxy memiliki kasus edge — properti yang ditambahkan secara dinamis mungkin memerlukan pembungkus reaktif eksplisit. Ekosistem Vue lebih kecil dari React dalam hal absolut."
        },
        related: ["after-hours://knowledge/javascript", "after-hours://knowledge/typescript", "after-hours://knowledge/react", "after-hours://knowledge/nextjs"]
    },
    {
        path: "after-hours://knowledge/nextjs",
        title: "Next.js",
        category: "knowledge",
        status: "FILED",
        version: "14.x",
        filed: "2026-05-26",
        role: "React-based full-stack web application framework.",
        language: "JavaScript / TypeScript",
        environment: "Browser / Node.js",
        installedInProject: false,
        dependencies: ["react", "react-dom"],
        tags: ["framework", "ssr", "routing", "fullstack"],
        content: {
            en: "FUNCTION\n\nNext.js is a framework built on top of React that adds server-side rendering (SSR), static site generation (SSG), file-system-based routing, incremental static regeneration (ISR), and API routes to the standard React model. It enables React applications to render HTML on the server before delivering it to the browser, which improves initial load performance and search engine indexability. This workstation does not use Next.js." + DIV + "CORE CONCEPTS\n\nNext.js defines routes through the file system. In the Pages Router (Next.js 12 and earlier), a file at pages/about.tsx maps to the URL /about. In the App Router (introduced in Next.js 13), routes are defined through directories with layout.tsx, page.tsx, loading.tsx, and error.tsx files, enabling nested layouts that persist across navigations.\n\nEach page can export a data-fetching function that determines how the page is generated. getStaticProps runs at build time and produces a static HTML file. getServerSideProps runs on every request and generates HTML dynamically on the server. Pages without either export are rendered as fully static. This mixture of rendering strategies within one application is a core feature of the framework.\n\nAPI routes (pages/api/... or app/api/...) define serverless handler functions that are deployed alongside the frontend. React Server Components (introduced in Next.js 13 App Router) allow components to run exclusively on the server, reducing client-side JavaScript bundle size." + DIV + "INTEGRATION CONTEXT\n\nNext.js is commonly adopted when a React project requires server-rendered HTML for SEO, a backend API layer without a separate service, or static generation with dynamic fallback. It is the primary full-stack React framework and competes in the same space as Nuxt.js (for Vue) and SvelteKit (for Svelte). Vercel, the company that maintains Next.js, operates the primary deployment platform optimized for it, though the framework can be deployed elsewhere." + DIV + "TRADEOFFS\n\nNext.js adds substantial framework-specific conventions to a React project. The routing model, rendering mode selection, data fetching patterns, and build output format are all Next.js-specific concerns. Migrating a large Next.js application to a different framework or to plain React is non-trivial.\n\nThe coexistence of Pages Router and App Router in Next.js 13 and 14 creates migration complexity within teams. React Server Components introduce a fundamental distinction between server components (no hooks, no browser APIs) and client components (require 'use client' directive) that developers must reason about throughout the codebase.",
            id: "FUNGSI\n\nNext.js adalah framework yang dibangun di atas React yang menambahkan server-side rendering (SSR), static site generation (SSG), routing berbasis file system, dan API routes ke model React standar. Workstation ini tidak menggunakan Next.js." + DIV + "KONSEP INTI\n\nNext.js mendefinisikan route melalui file system. App Router (Next.js 13) memperkenalkan layout bertingkat dan React Server Components. Setiap halaman dapat mengekspor fungsi pengambilan data yang menentukan cara halaman dibuat: getStaticProps pada waktu build, getServerSideProps pada setiap permintaan." + DIV + "KONTEKS INTEGRASI\n\nNext.js umumnya diadopsi ketika proyek React memerlukan HTML yang dirender server untuk SEO, lapisan API backend tanpa layanan terpisah, atau pembuatan statis dengan fallback dinamis. Ini adalah framework React full-stack utama dan bersaing dengan Nuxt.js dan SvelteKit." + DIV + "TRADEOFF\n\nNext.js menambahkan konvensi khusus framework yang substansial ke proyek React. Migrasi aplikasi Next.js besar ke framework yang berbeda tidak sepele. Koeksistensi Pages Router dan App Router menciptakan kompleksitas migrasi."
        },
        related: ["after-hours://knowledge/react", "after-hours://knowledge/nodejs", "after-hours://knowledge/rest-api"]
    },
    {
        path: "after-hours://knowledge/nodejs",
        title: "Node.js",
        category: "knowledge",
        status: "FILED",
        version: "20.x LTS",
        filed: "2026-05-26",
        role: "Server-side JavaScript runtime environment.",
        language: "JavaScript / TypeScript",
        environment: "Server / Build-time",
        installedInProject: true,
        dependencies: [],
        tags: ["runtime", "server", "npm", "javascript"],
        content: {
            en: "FUNCTION\n\nNode.js is a JavaScript runtime built on V8, Chrome's JavaScript engine, that executes JavaScript outside the browser. It provides access to the operating system's file system, network interfaces, process management, and standard I/O streams — capabilities unavailable to browser JavaScript for security reasons. Node.js is the runtime that powers the npm and pnpm package managers, almost all JavaScript build tools, and the majority of backend JavaScript services." + DIV + "CORE CONCEPTS\n\nNode.js uses the same event loop model as browser JavaScript. Asynchronous I/O operations (reading a file, making a network request) are handed off to the operating system and their callbacks are queued when complete. This allows a single Node.js process to handle many concurrent I/O operations without spawning threads for each.\n\nNode.js supports two module systems. CommonJS (the original system) uses require() and module.exports. ES Modules (the modern system) uses import and export. Both can coexist in a project, but require careful configuration of package.json and tsconfig.json to avoid interop conflicts.\n\nThe npm registry hosts hundreds of thousands of publicly available packages. The package.json file declares project dependencies, development dependencies, scripts, and metadata. The node_modules directory contains installed packages and their transitive dependencies. The package-lock.json file records exact resolved versions for reproducible installs." + DIV + "INTEGRATION CONTEXT\n\nThis workstation uses Node.js as the local development runtime for both workspaces. The workspace-os build is driven by react-scripts, which internally uses a Node.js-based Webpack configuration. The workspace-3d build uses a custom Webpack configuration and ts-loader, both running under Node.js. The Express static file server (workspace-3d/server) is a Node.js process.\n\nIn production, Node.js is not part of the deployed runtime. Both workspaces produce static file bundles that are served from a CDN. Node.js remains a build-time and development-time dependency only." + DIV + "TRADEOFFS\n\nNode.js's event loop is highly efficient for I/O-bound workloads — handling many concurrent network connections, file reads, or database queries with low overhead. It is not suitable for CPU-intensive tasks such as image processing, cryptographic computation, or scientific simulation without offloading work to native C++ addons or Worker threads.\n\nThe npm ecosystem's scale is both an asset and a liability. Dependency trees grow deep and wide, increasing the surface area for supply-chain vulnerabilities. Large node_modules directories introduce disk usage and install time overhead. Lockfile discipline (committing package-lock.json or pnpm-lock.yaml) is essential for reproducible builds across environments.",
            id: "FUNGSI\n\nNode.js adalah runtime JavaScript yang dibangun di atas V8, mesin JavaScript Chrome, yang mengeksekusi JavaScript di luar browser. Menyediakan akses ke file system OS, antarmuka jaringan, manajemen proses, dan stream I/O standar. Node.js adalah runtime yang mendukung manajer paket npm dan pnpm, hampir semua alat build JavaScript." + DIV + "KONSEP INTI\n\nNode.js menggunakan model event loop yang sama dengan JavaScript browser. Operasi I/O asinkron diserahkan ke sistem operasi dan callback-nya diantrekan ketika selesai. Node.js mendukung dua sistem modul: CommonJS (require/module.exports) dan ES Modules (import/export). Package.json mendeklarasikan dependensi proyek, skrip, dan metadata." + DIV + "KONTEKS INTEGRASI\n\nWorkstation ini menggunakan Node.js sebagai runtime pengembangan lokal untuk kedua workspace. Build workspace-os digerakkan oleh react-scripts. Build workspace-3d menggunakan konfigurasi Webpack kustom. Server file statis Express adalah proses Node.js. Dalam produksi, Node.js bukan bagian dari runtime yang di-deploy." + DIV + "TRADEOFF\n\nEvent loop Node.js sangat efisien untuk beban kerja I/O-bound tetapi tidak cocok untuk tugas intensif CPU tanpa Worker threads atau addon native. Ekosistem npm yang besar meningkatkan area permukaan untuk kerentanan supply-chain. Direktori node_modules yang besar menambah overhead disk dan waktu instalasi."
        },
        related: ["after-hours://knowledge/javascript", "after-hours://knowledge/nestjs", "after-hours://knowledge/rest-api", "after-hours://stack/express"]
    },
    {
        path: "after-hours://knowledge/nestjs",
        title: "NestJS",
        category: "knowledge",
        status: "FILED",
        version: "10.x",
        filed: "2026-05-26",
        role: "TypeScript-first backend framework for Node.js.",
        language: "TypeScript",
        environment: "Node.js",
        installedInProject: false,
        dependencies: [],
        tags: ["backend", "framework", "typescript", "dependency-injection"],
        content: {
            en: "FUNCTION\n\nNestJS is a TypeScript-first backend framework for Node.js applications. It imposes architectural structure on Node.js backend services through modules, controllers, services, providers, and dependency injection. This record exists as service architecture reference. This workstation does not use NestJS as its installed backend layer — the workspace server operates on Express middleware only." + DIV + "CORE CONCEPTS\n\nNestJS uses TypeScript decorators to define the architectural role of each class. The @Controller() decorator marks a class as an HTTP request handler and specifies its base route path. The @Get(), @Post(), @Put(), @Delete() decorators on methods define the HTTP method and sub-path for each endpoint. The @Injectable() decorator marks a class as a provider — a value that can be injected as a dependency.\n\nDependency injection (DI) is central to NestJS. Instead of importing service instances directly, a class declares its dependencies in its constructor. The NestJS IoC (Inversion of Control) container resolves and injects the correct instances at runtime, based on module declarations. This makes components independently testable by substituting mock implementations.\n\nModules (@Module()) group related controllers, providers, and imports. Each module declares what it exports to other modules and what it imports from them. This explicit dependency graph keeps large codebases organized. The AppModule is the root module; all other modules are imported (directly or transitively) from it.\n\nNestJS can run on Express or Fastify as its HTTP transport layer. It includes built-in support for WebSockets, GraphQL (via @nestjs/graphql), and microservice patterns (via @nestjs/microservices)." + DIV + "INTEGRATION CONTEXT\n\nNestJS is commonly adopted by teams building medium-to-large scale Node.js backend APIs, particularly those migrating from Angular or familiar with enterprise Java patterns (Spring Boot's architecture is a clear analog). It pairs naturally with PostgreSQL through TypeORM or Prisma, and with REST or GraphQL API patterns.\n\nIn a typical full-stack TypeScript project, NestJS handles the backend API, Next.js or a frontend framework handles the client, and both share TypeScript type definitions through a shared package or a monorepo structure." + DIV + "TRADEOFFS\n\nNestJS introduces significant boilerplate relative to a minimal Express setup. A simple CRUD endpoint requires a module, controller, service, and DTO class with decorator annotations. For small services, this overhead exceeds the organizational benefit.\n\nThe decorator and DI patterns assume familiarity with Angular-style architecture. Developers accustomed to functional middleware patterns (Express, Fastify, Hono) face a steeper learning curve. The framework's abstraction layers can also make debugging harder — a DI resolution error or decorator misconfiguration produces errors that require understanding the NestJS lifecycle to interpret.",
            id: "FUNGSI\n\nNestJS adalah framework backend TypeScript-first untuk aplikasi Node.js. Framework ini memaksakan struktur arsitektur pada layanan backend Node.js melalui modul, controller, service, provider, dan dependency injection. Catatan ini ada sebagai referensi arsitektur layanan. Workstation ini tidak menggunakan NestJS." + DIV + "KONSEP INTI\n\nNestJS menggunakan dekorator TypeScript untuk mendefinisikan peran arsitektur setiap kelas: @Controller(), @Injectable(), @Module(). Dependency injection adalah inti dari NestJS — alih-alih mengimpor instance layanan secara langsung, sebuah kelas mendeklarasikan dependensinya di konstruktornya dan container IoC NestJS menyelesaikan dan menyuntikkan instance yang benar." + DIV + "KONTEKS INTEGRASI\n\nNestJS umumnya diadopsi oleh tim yang membangun API backend Node.js skala menengah hingga besar. NestJS berpasangan secara alami dengan PostgreSQL melalui TypeORM atau Prisma, dan dengan pola REST atau GraphQL API." + DIV + "TRADEOFF\n\nNestJS memperkenalkan boilerplate yang signifikan relatif terhadap setup Express minimal. Pola DI dan dekorator mengasumsikan keakraban dengan arsitektur gaya Angular. Lapisan abstraksi framework dapat membuat debugging lebih sulit."
        },
        related: ["after-hours://knowledge/nodejs", "after-hours://knowledge/typescript", "after-hours://knowledge/rest-api", "after-hours://knowledge/postgresql"]
    },
    {
        path: "after-hours://knowledge/rest-api",
        title: "REST API",
        category: "knowledge",
        status: "FILED",
        version: "—",
        filed: "2026-05-26",
        role: "Architectural pattern for HTTP-based service interfaces.",
        language: "—",
        environment: "HTTP",
        installedInProject: false,
        dependencies: [],
        tags: ["api", "http", "architecture", "stateless"],
        content: {
            en: "FUNCTION\n\nREST (Representational State Transfer) is an architectural style for designing networked service interfaces over HTTP. A REST API exposes resources as URLs and uses standard HTTP methods to express the intended operation on those resources. REST is a set of constraints, not a formal specification — implementations vary in how strictly they adhere to all six REST constraints defined by Roy Fielding in his 2000 dissertation." + DIV + "CORE CONCEPTS\n\nResources are identified by URLs: /users identifies the users collection, /users/42 identifies the user with ID 42. HTTP methods carry semantic meaning: GET retrieves a resource without side effects, POST creates a new resource or triggers an action, PUT replaces a resource in full, PATCH partially updates a resource, DELETE removes a resource.\n\nHTTP status codes communicate the result: 200 OK (success with response body), 201 Created (resource created), 204 No Content (success with no body), 400 Bad Request (malformed input), 401 Unauthorized (missing authentication), 403 Forbidden (insufficient permission), 404 Not Found (resource does not exist), 409 Conflict (state conflict), 422 Unprocessable Entity (validation failure), 500 Internal Server Error.\n\nREST is stateless — each request must include all information needed to process it. The server retains no session memory between requests. Authentication is typically passed in request headers using the Authorization header, most commonly with the Bearer token scheme carrying a JWT.\n\nRequest and response bodies are typically formatted as JSON. Content negotiation (Accept and Content-Type headers) allows clients and servers to agree on the data format." + DIV + "INTEGRATION CONTEXT\n\nREST APIs serve as the interface boundary between frontend clients and backend services. A web frontend fetches data from REST endpoints using the fetch API or a library like Axios, renders the response into the interface, and submits user actions back to the server through POST or PUT requests. Mobile apps, third-party integrations, and microservices consume the same REST endpoints.\n\nThis workstation does not expose a REST API. The archive data is stored as local TypeScript modules and requires no network request. A production system of comparable scope would use REST endpoints to serve dynamic content, user authentication, and data persistence." + DIV + "TRADEOFFS\n\nREST is widely understood, supported by all HTTP tooling, and easy to debug with standard browser developer tools or tools like curl and Postman. Its constraints (statelessness, resource orientation) promote scalability and loose coupling between client and server.\n\nHowever, REST API design requires discipline. Over-fetching (receiving more data than the client needs) and under-fetching (requiring multiple requests to assemble related data) are common inefficiencies. Versioning REST APIs across breaking changes is a significant operational concern. GraphQL was developed in part to address over-fetching and under-fetching by allowing clients to specify exactly what data they require.",
            id: "FUNGSI\n\nREST (Representational State Transfer) adalah gaya arsitektur untuk merancang antarmuka layanan jaringan melalui HTTP. REST API mengekspos resource sebagai URL dan menggunakan metode HTTP standar untuk mengekspresikan operasi yang dimaksud." + DIV + "KONSEP INTI\n\nResource diidentifikasi oleh URL. Metode HTTP membawa makna semantik: GET mengambil, POST membuat, PUT mengganti penuh, PATCH memperbarui sebagian, DELETE menghapus. Kode status HTTP mengkomunikasikan hasilnya. REST bersifat stateless — setiap permintaan harus menyertakan semua informasi yang diperlukan untuk memprosesnya. Autentikasi biasanya diteruskan di header permintaan menggunakan skema Bearer token." + DIV + "KONTEKS INTEGRASI\n\nREST API berfungsi sebagai batas antarmuka antara klien frontend dan layanan backend. Workstation ini tidak mengekspos REST API. Data arsip disimpan sebagai modul TypeScript lokal dan tidak memerlukan permintaan jaringan." + DIV + "TRADEOFF\n\nREST dipahami secara luas dan mudah di-debug dengan alat HTTP standar. Namun, over-fetching dan under-fetching adalah inefisiensi umum. GraphQL dikembangkan sebagian untuk mengatasi masalah ini dengan mengizinkan klien menentukan dengan tepat data apa yang mereka butuhkan."
        },
        related: ["after-hours://knowledge/nodejs", "after-hours://knowledge/nestjs", "after-hours://knowledge/auth", "after-hours://knowledge/postgresql"]
    },
    {
        path: "after-hours://knowledge/sql",
        title: "SQL",
        category: "knowledge",
        status: "FILED",
        version: "—",
        filed: "2026-05-26",
        role: "Standard query language for relational databases.",
        language: "SQL",
        environment: "Database",
        installedInProject: false,
        dependencies: [],
        tags: ["database", "query", "relational", "structured-data"],
        content: {
            en: "FUNCTION\n\nSQL (Structured Query Language) is the standard language for interacting with relational database management systems (RDBMS). It covers data definition (creating and modifying schemas), data manipulation (inserting, updating, and deleting rows), data querying (retrieving records based on conditions), and access control (granting and revoking permissions). SQL is declarative — a query describes what data to retrieve, not how the database engine should retrieve it. This workstation does not use SQL. The archive data is stored as local TypeScript modules." + DIV + "CORE CONCEPTS\n\nData is organized into tables — named collections of rows, where each row represents a record and each column represents a typed attribute. Tables are defined by a schema (CREATE TABLE) that specifies column names, data types, and constraints (NOT NULL, UNIQUE, PRIMARY KEY, FOREIGN KEY, CHECK).\n\nThe SELECT statement retrieves rows from one or more tables. The WHERE clause filters rows by condition. ORDER BY sorts results. GROUP BY aggregates rows by a shared value, enabling aggregate functions (COUNT, SUM, AVG, MAX, MIN). LIMIT restricts the number of returned rows.\n\nJOIN operations combine rows from multiple tables based on matching column values. An INNER JOIN returns only rows with matching values in both tables. A LEFT JOIN returns all rows from the left table and matching rows from the right. A FULL OUTER JOIN returns all rows from both tables.\n\nIndexes are auxiliary data structures (typically B-trees) that allow the database to locate rows matching a condition without scanning the full table. Indexes dramatically improve query performance on large tables but add overhead to write operations and increase storage consumption.\n\nTransactions group multiple statements into an atomic unit. Either all statements succeed (COMMIT) or all are rolled back to the prior state (ROLLBACK). Isolation levels (READ COMMITTED, REPEATABLE READ, SERIALIZABLE) control how concurrent transactions interact." + DIV + "INTEGRATION CONTEXT\n\nSQL is the data layer language for any application that requires persistent, structured, queryable storage. Web application backends typically use an ORM (Object-Relational Mapper) like Prisma, TypeORM, Sequelize, or SQLAlchemy to translate application-level code into SQL queries, abstracting the database-specific syntax. Direct SQL query builders (like Knex.js) provide more control with less abstraction.\n\nMajor RDBMS options include PostgreSQL (open-source, feature-rich), MySQL/MariaDB (common in web hosting stacks), SQLite (embedded, file-based, no server required), and Microsoft SQL Server (enterprise). Cloud providers offer managed versions of these databases, reducing operational overhead." + DIV + "TRADEOFFS\n\nRelational databases enforce referential integrity through foreign key constraints and schema validation, which prevents data corruption at the storage layer. This is a significant reliability advantage over application-level validation alone.\n\nHowever, the strict schema structure requires coordinated migrations when the data model changes. Schema migration tools (Flyway, Liquibase, Prisma Migrate, Alembic) manage version-controlled schema changes, but they add a deployment step that must be coordinated with application deployments.\n\nSQL is not naturally suited to highly nested, variable-structure, or document-oriented data. Storing JSON blobs in SQL columns (supported by PostgreSQL's JSONB) partially bridges this gap, but complex document hierarchies are better served by document databases (MongoDB, Firestore). Horizontal scaling of writes across multiple nodes requires sharding strategies that add operational complexity.",
            id: "FUNGSI\n\nSQL (Structured Query Language) adalah bahasa standar untuk berinteraksi dengan sistem manajemen basis data relasional (RDBMS). SQL mencakup definisi data, manipulasi data, kueri data, dan kontrol akses. SQL bersifat deklaratif — kueri mendeskripsikan data apa yang akan diambil, bukan bagaimana mesin database harus mengambilnya. Workstation ini tidak menggunakan SQL." + DIV + "KONSEP INTI\n\nData diorganisir ke dalam tabel — koleksi baris bernama di mana setiap baris mewakili catatan. Pernyataan SELECT mengambil baris dari satu atau lebih tabel. Operasi JOIN menggabungkan baris dari beberapa tabel berdasarkan nilai kolom yang cocok. Indeks adalah struktur data pembantu yang memungkinkan database menemukan baris yang cocok dengan kondisi tanpa memindai seluruh tabel. Transaksi mengelompokkan beberapa pernyataan ke dalam unit atomik." + DIV + "KONTEKS INTEGRASI\n\nSQL adalah bahasa lapisan data untuk aplikasi apa pun yang memerlukan penyimpanan persisten, terstruktur, dan dapat dikueri. Backend aplikasi web biasanya menggunakan ORM seperti Prisma, TypeORM, atau Sequelize untuk menerjemahkan kode tingkat aplikasi ke kueri SQL." + DIV + "TRADEOFF\n\nDatabase relasional menerapkan integritas referensial melalui batasan foreign key dan validasi skema. Namun, struktur skema yang ketat memerlukan migrasi terkoordinasi ketika model data berubah. SQL tidak cocok secara alami untuk data yang sangat bersarang atau berorientasi dokumen."
        },
        related: ["after-hours://knowledge/postgresql", "after-hours://knowledge/rest-api", "after-hours://knowledge/auth"]
    },
    {
        path: "after-hours://knowledge/postgresql",
        title: "PostgreSQL",
        category: "knowledge",
        status: "FILED",
        version: "16.x",
        filed: "2026-05-26",
        role: "Open-source relational database management system.",
        language: "SQL / PL/pgSQL",
        environment: "Server",
        installedInProject: false,
        dependencies: [],
        tags: ["database", "relational", "open-source", "acid"],
        content: {
            en: "FUNCTION\n\nPostgreSQL is an open-source, ACID-compliant relational database management system. It extends the SQL standard with a broad set of additional capabilities: advanced data types, full-text search, native JSON storage, geospatial indexing (via PostGIS), and extensibility through custom functions and extensions. PostgreSQL is the most widely adopted open-source RDBMS for production web application backends. This workstation does not use PostgreSQL." + DIV + "CORE CONCEPTS\n\nACID compliance (Atomicity, Consistency, Isolation, Durability) ensures that database operations behave reliably under failure conditions. Atomicity guarantees that a transaction either completes fully or not at all. Consistency ensures that each transaction leaves the database in a valid state. Isolation controls how concurrent transactions interact — PostgreSQL supports multiple isolation levels. Durability guarantees that committed transactions survive crashes.\n\nPostgreSQL supports JSONB columns, which store JSON data in a binary format that allows indexed queries into the document structure — a hybrid between relational and document storage. Full-text search indexes allow efficient keyword queries over large text fields without an external search engine.\n\nSchemas organize tables, views, functions, and indexes into named namespaces within a database. A single PostgreSQL server can host multiple databases, each with multiple schemas. Row-level security (RLS) policies allow fine-grained access control: a policy can restrict which rows a user can read or modify based on their identity.\n\nMigrations track and apply schema changes over time. Tools like Prisma Migrate, Flyway, and Liquibase generate versioned migration files that can be reviewed, tested, and applied in sequence across environments." + DIV + "INTEGRATION CONTEXT\n\nPostgreSQL is commonly integrated with Node.js backends through the node-postgres (pg) library for direct SQL queries or through ORMs (Prisma, TypeORM, Drizzle) that abstract the query layer. In the NestJS ecosystem, TypeORM and Prisma are the primary choices. Python backends use psycopg2 or SQLAlchemy.\n\nMost cloud platforms offer managed PostgreSQL services: Amazon RDS, Google Cloud SQL, Azure Database for PostgreSQL, Supabase, and Neon. Managed services handle provisioning, backups, failover, and version upgrades, reducing the operational overhead of running a self-hosted server." + DIV + "TRADEOFFS\n\nPostgreSQL offers strong data integrity guarantees, rich query capabilities, and a broad extension ecosystem. It is the default choice for complex domain models that require relational constraints, multi-table joins, or transactional consistency across multiple operations.\n\nHowever, PostgreSQL requires a running database server process with allocated memory and storage. Unlike SQLite (a file-based embedded database), PostgreSQL cannot be bundled with the application. Schema migrations introduce a deployment coordination step — application code and schema changes must be deployed in the correct order to avoid runtime errors during rollouts.\n\nHorizontal write scaling requires sharding or a distributed extension (Citus), which adds substantial operational complexity. Read scaling through read replicas is straightforward but introduces eventual consistency for replica reads.",
            id: "FUNGSI\n\nPostgreSQL adalah sistem manajemen basis data relasional open-source yang sesuai ACID. PostgreSQL memperluas standar SQL dengan kemampuan tambahan yang luas: tipe data lanjutan, pencarian teks penuh, penyimpanan JSON native, dan ekstensibilitas melalui fungsi dan ekstensi kustom. Workstation ini tidak menggunakan PostgreSQL." + DIV + "KONSEP INTI\n\nACID compliance memastikan bahwa operasi database berperilaku andal dalam kondisi kegagalan. PostgreSQL mendukung kolom JSONB, yang menyimpan data JSON dalam format biner yang memungkinkan kueri terindeks ke dalam struktur dokumen. Kebijakan keamanan tingkat baris (RLS) memungkinkan kontrol akses berbutir halus. Migrasi melacak dan menerapkan perubahan skema dari waktu ke waktu." + DIV + "KONTEKS INTEGRASI\n\nPostgreSQL umumnya diintegrasikan dengan backend Node.js melalui library node-postgres atau melalui ORM (Prisma, TypeORM, Drizzle). Sebagian besar platform cloud menawarkan layanan PostgreSQL terkelola: Amazon RDS, Google Cloud SQL, Supabase, dan Neon." + DIV + "TRADEOFF\n\nPostgreSQL memerlukan proses server database yang berjalan dengan memori dan penyimpanan yang dialokasikan. Migrasi skema memperkenalkan langkah koordinasi deployment. Penskalaan horizontal penulisan memerlukan sharding atau ekstensi terdistribusi, yang menambah kompleksitas operasional yang substansial."
        },
        related: ["after-hours://knowledge/sql", "after-hours://knowledge/nestjs", "after-hours://knowledge/rest-api", "after-hours://knowledge/auth"]
    },
    {
        path: "after-hours://knowledge/auth",
        title: "Authentication & Authorization",
        category: "knowledge",
        status: "FILED",
        version: "—",
        filed: "2026-05-26",
        role: "Patterns for verifying identity and controlling access.",
        language: "—",
        environment: "Application-layer",
        installedInProject: false,
        dependencies: [],
        tags: ["security", "auth", "jwt", "sessions"],
        content: {
            en: "FUNCTION\n\nAuthentication (authn) is the process of verifying the identity of a user or system — confirming that they are who they claim to be. Authorization (authz) is the process of determining what an authenticated identity is permitted to do — what resources they can access and what operations they can perform. These are distinct concerns that are frequently confused but must be implemented separately. This workstation does not have user accounts or authentication." + DIV + "CORE CONCEPTS\n\nSession-based authentication: on successful login, the server creates a session record in its storage (memory, database, or Redis) and issues the client a session ID, typically stored in an httpOnly cookie. Each subsequent request includes the cookie, and the server resolves the session ID to the user identity. Session revocation is straightforward — deleting the record immediately invalidates the session.\n\nToken-based authentication using JSON Web Tokens (JWT): on login, the server generates a signed token encoding the user's identity and claims (user ID, roles, expiry timestamp). The signature uses a secret key (HMAC-SHA256) or a private key (RS256). The client stores the token and includes it in subsequent requests as an Authorization: Bearer header. The server verifies the signature without a database lookup, making this approach stateless and horizontally scalable.\n\nOAuth 2.0 is a delegation protocol that allows a user to grant a third-party application limited access to their account on another service, without sharing their password. The user authenticates with the identity provider (Google, GitHub, Microsoft), which issues an access token to the requesting application. OpenID Connect (OIDC) extends OAuth 2.0 to include identity verification — the ID token contains verified claims about the authenticated user.\n\nRole-Based Access Control (RBAC) assigns permissions to roles, and roles to users. A user with the 'admin' role may access routes that a 'viewer' role cannot. Attribute-Based Access Control (ABAC) evaluates more granular conditions: a user may edit a resource only if they created it." + DIV + "INTEGRATION CONTEXT\n\nAuthentication is typically enforced at the application server layer through middleware that inspects each incoming request before it reaches business logic. In Express, Passport.js provides a strategy-based authentication framework supporting dozens of methods (JWT, OAuth, local username/password). In NestJS, Passport is integrated through the @nestjs/passport module with guards that protect routes declaratively. In Next.js, the Auth.js library (formerly NextAuth.js) handles session management, provider integration, and database adapters.\n\nSession storage requires either a sessions table in the application database or an external store like Redis for distributed environments where multiple server instances must share session state." + DIV + "TRADEOFFS\n\nSessions are easy to revoke immediately and do not expose long-lived credentials to the client. The tradeoff is server-side storage that must scale with the active user count and be shared across all server instances in a horizontally scaled deployment.\n\nJWTs are stateless and require no server-side storage lookup. Verification is fast and works across distributed systems without shared state. The critical limitation is revocation: a JWT cannot be invalidated before its expiry time without maintaining a token denylist, which reintroduces server-side state. Short expiry times (15 minutes) mitigate this but require refresh token rotation logic.\n\nStoring tokens in localStorage exposes them to XSS attacks — a malicious script injected into the page can read and exfiltrate the token. HttpOnly cookies are inaccessible to JavaScript and are the preferred storage for session IDs and refresh tokens. The correct security posture depends on the threat model of the application.",
            id: "FUNGSI\n\nAuthentication (authn) adalah proses memverifikasi identitas pengguna atau sistem. Authorization (authz) adalah proses menentukan apa yang diizinkan identitas yang terautentikasi untuk dilakukan. Ini adalah kekhawatiran yang berbeda yang harus diimplementasikan secara terpisah. Workstation ini tidak memiliki akun pengguna atau autentikasi." + DIV + "KONSEP INTI\n\nAutentikasi berbasis sesi: pada login yang berhasil, server membuat catatan sesi dan mengeluarkan ID sesi ke klien, biasanya disimpan dalam cookie httpOnly. Autentikasi berbasis token menggunakan JWT: pada login, server menghasilkan token yang ditandatangani yang mengkodekan identitas dan klaim pengguna. OAuth 2.0 adalah protokol delegasi yang memungkinkan pengguna memberikan akses terbatas kepada aplikasi pihak ketiga ke akun mereka di layanan lain. RBAC menetapkan izin ke peran, dan peran ke pengguna." + DIV + "KONTEKS INTEGRASI\n\nAutentikasi biasanya diterapkan di lapisan server aplikasi melalui middleware yang memeriksa setiap permintaan masuk. Di NestJS, Passport.js diintegrasikan melalui modul @nestjs/passport. Di Next.js, library Auth.js menangani manajemen sesi. Penyimpanan sesi memerlukan tabel sesi di database atau penyimpanan eksternal seperti Redis." + DIV + "TRADEOFF\n\nSesi mudah dibatalkan segera tetapi memerlukan penyimpanan sisi server yang harus diskalakan. JWT bersifat stateless tetapi tidak dapat dibatalkan sebelum waktu kedaluwarsa tanpa mempertahankan denylist token. Menyimpan token di localStorage mengekspos mereka ke serangan XSS. Cookie HttpOnly tidak dapat diakses oleh JavaScript dan merupakan penyimpanan yang lebih disukai."
        },
        related: ["after-hours://knowledge/rest-api", "after-hours://knowledge/sql", "after-hours://knowledge/postgresql", "after-hours://knowledge/nodejs"]
    },
    {
        path: "after-hours://knowledge/git",
        title: "Git",
        category: "knowledge",
        status: "FILED",
        version: "2.x",
        filed: "2026-05-26",
        role: "Distributed version control system.",
        language: "—",
        environment: "CLI / Build-time",
        installedInProject: true,
        dependencies: [],
        tags: ["version-control", "collaboration", "branching", "history"],
        content: {
            en: "FUNCTION\n\nGit is a distributed version control system that records changes to files over time and enables multiple contributors to coordinate work on a shared codebase. Unlike centralized version control systems (CVS, Subversion), Git gives every working directory a complete copy of the repository, including its full history. This means most operations (browsing history, creating branches, reviewing diffs) are local and do not require network access." + DIV + "CORE CONCEPTS\n\nA commit is a snapshot of the entire tracked file tree at a point in time, identified by a SHA-1 hash. Each commit records its parent commit(s), forming a directed acyclic graph of snapshots. The graph structure allows Git to represent both linear history and branching/merging events.\n\nA branch is a named pointer to a specific commit. Creating a new branch costs almost nothing — it creates a single reference file. Branches allow parallel lines of development to proceed independently. The HEAD pointer tracks the currently checked-out branch or commit.\n\nMerging integrates the history of two branches. A fast-forward merge advances the target branch pointer without creating a new commit, if no divergence exists. A three-way merge creates a new merge commit with two parents. Rebasing rewrites one branch's commits to appear as if they were made on top of another branch's tip, producing a linear history at the cost of rewriting commit hashes.\n\nThe staging area (index) allows selective inclusion of changes in a commit. A modified file is not automatically included in the next commit — changes must be explicitly staged with git add. This enables granular, logically organized commits.\n\nRemote repositories (hosted on GitHub, GitLab, Bitbucket, or self-hosted) serve as the shared coordination point. git push sends local commits to the remote. git fetch downloads remote commits without merging. git pull combines fetch and merge (or rebase, depending on configuration)." + DIV + "INTEGRATION CONTEXT\n\nThis project uses Git for version control across all workspaces. GitHub hosts the remote repository. The development workflow follows a feature branch model: work begins on a dedicated branch, commits are pushed to the remote, a pull request is opened for review, and the branch is merged into the main branch after approval. CI/CD pipelines are triggered by push events or pull request merges.\n\nGit hooks (pre-commit, pre-push) allow automated checks to run locally before operations complete. Hooks can enforce linting, test execution, or commit message format requirements." + DIV + "TRADEOFFS\n\nGit's branching and merging model gives teams flexibility for parallel development, but it introduces coordination overhead. Merge conflicts occur when two branches modify the same lines of code and require manual resolution. Long-lived feature branches increase the probability of conflicts with the main branch.\n\nGit tracks text-based diffs efficiently but handles binary files poorly. Large binary assets (images, audio files, 3D models, compiled artifacts) are not well-suited to Git's delta storage model and cause repository size to balloon over time. Git LFS (Large File Storage) replaces binary file content with pointers, storing the actual data on a separate server.\n\nThe Git command line interface has a non-trivial learning curve. Common operations are straightforward, but advanced scenarios (rebasing across many commits, recovering from detached HEAD state, understanding the reflog) require conceptual understanding of the underlying data model.",
            id: "FUNGSI\n\nGit adalah sistem kontrol versi terdistribusi yang merekam perubahan pada file dari waktu ke waktu dan memungkinkan beberapa kontributor untuk mengoordinasikan pekerjaan pada codebase bersama. Tidak seperti sistem kontrol versi terpusat, Git memberikan setiap direktori kerja salinan lengkap dari repository, termasuk seluruh sejarahnya." + DIV + "KONSEP INTI\n\nCommit adalah snapshot dari seluruh pohon file yang dilacak pada suatu titik waktu, diidentifikasi oleh hash SHA-1. Branch adalah pointer bernama ke commit tertentu. Merging mengintegrasikan sejarah dua branch. Rebasing menulis ulang commit satu branch agar tampak seolah-olah dibuat di atas ujung branch lain. Area staging (index) memungkinkan penyertaan selektif perubahan dalam commit." + DIV + "KONTEKS INTEGRASI\n\nProyek ini menggunakan Git untuk kontrol versi di semua workspace. GitHub menjadi host repository remote. Alur kerja pengembangan mengikuti model feature branch: pekerjaan dimulai di branch khusus, commit didorong ke remote, pull request dibuka untuk ditinjau, dan branch digabungkan ke branch utama setelah persetujuan." + DIV + "TRADEOFF\n\nModel branching dan merging Git memberi tim fleksibilitas untuk pengembangan paralel, tetapi memperkenalkan overhead koordinasi. Konflik merge terjadi ketika dua branch memodifikasi baris kode yang sama dan memerlukan resolusi manual. Git menangani file biner dengan buruk — Git LFS menggantikan konten file biner dengan pointer."
        },
        related: ["after-hours://knowledge/deployment", "after-hours://knowledge/nodejs"]
    },
    {
        path: "after-hours://knowledge/deployment",
        title: "Deployment",
        category: "knowledge",
        status: "FILED",
        version: "—",
        filed: "2026-05-26",
        role: "Process of making a software system available to users.",
        language: "—",
        environment: "Infrastructure",
        installedInProject: false,
        dependencies: [],
        tags: ["infrastructure", "ci-cd", "hosting", "build"],
        content: {
            en: "FUNCTION\n\nDeployment is the process of building, packaging, and distributing a software system so that it runs in an environment accessible to its intended users. It encompasses every step between a developer committing code and that code serving real traffic: the build pipeline, artifact packaging, infrastructure provisioning, release coordination, and rollback capability. Deployment strategy is an architectural concern — the choice of approach constrains what the application can do at runtime and how quickly defects can be corrected." + DIV + "CORE CONCEPTS\n\nCI/CD (Continuous Integration / Continuous Deployment) automates the path from code commit to running production environment. Continuous Integration means every commit triggers an automated pipeline that builds the application, runs tests, and performs static analysis. Continuous Deployment extends this by automatically deploying the verified build artifact to production without human approval. Continuous Delivery is the intermediate approach: the pipeline produces a deployable artifact automatically, but deployment requires a manual approval step.\n\nStatic site deployment: for frontend applications with no server-side execution, the build process produces a set of static files (HTML, CSS, JavaScript, images). These files are uploaded to a CDN (Content Delivery Network) which replicates them across edge nodes geographically distributed close to users. Requests are served from the nearest edge, minimizing latency. This model scales automatically and costs very little at low traffic volumes.\n\nContainer-based deployment: application code and all its dependencies are packaged into a Docker image — a portable, reproducible execution environment. The image is pushed to a container registry. A container orchestration system (Kubernetes, Amazon ECS, Google Cloud Run) pulls the image and manages running container instances, handling scaling, health checks, service discovery, and rolling updates.\n\nServerless deployment: individual functions are deployed as isolated units of execution. The cloud provider handles all infrastructure — provisioning, scaling, and decommissioning. The function executes in response to an event (HTTP request, queue message, schedule) and terminates when done. Billing is per-execution rather than per-server." + DIV + "INTEGRATION CONTEXT\n\nThe AFTER-HOURS workstation is deployed as two static sites. The React OS (workspace-os build output) is served from a subdomain. The 3D room entry point (workspace-3d build output) is served from the main domain. A build specification file defines the CI/CD pipeline steps: install dependencies, run the build command, and upload the output to the hosting service. Both build outputs are static files that require no server-side runtime.\n\nLocal development serves the builds through dev servers (react-scripts start for workspace-os, webpack-dev-server for workspace-3d) that support hot module replacement and live reload. The production builds differ from development builds in that they include minification, tree shaking, content-addressable filenames, and no source maps." + DIV + "TRADEOFFS\n\nStatic deployment is the simplest and most cost-effective option at small to medium scale. It requires no server management and scales automatically. The constraint is that all logic must execute in the browser — there is no server-side code path available. Authentication, database access, and private API integration require a separate backend service.\n\nContainer-based deployment supports any runtime workload but introduces operational complexity: container registry management, orchestration configuration, health check design, rolling update strategies, and infrastructure cost management. The operational overhead is justified when the application requires server-side execution, persistent background processes, or workloads that cannot run in the browser.\n\nCI/CD pipelines add a mandatory delay between code commit and production deployment. This tradeoff is accepted because automated build and test verification reduces the probability of deploying broken code. Pipeline failures must be diagnosed and resolved before deployment proceeds, which requires discipline in keeping the build green.",
            id: "FUNGSI\n\nDeployment adalah proses membangun, mengemas, dan mendistribusikan sistem perangkat lunak sehingga berjalan di lingkungan yang dapat diakses oleh pengguna yang dituju. Deployment mencakup setiap langkah antara commit developer dan kode yang melayani traffic nyata." + DIV + "KONSEP INTI\n\nCI/CD mengotomatiskan jalur dari commit kode ke lingkungan produksi yang berjalan. Deployment situs statis: untuk aplikasi frontend tanpa eksekusi sisi server, proses build menghasilkan sekumpulan file statis yang diunggah ke CDN. Deployment berbasis container: kode aplikasi dan semua dependensinya dikemas ke dalam image Docker. Deployment serverless: fungsi individual di-deploy sebagai unit eksekusi yang terisolasi." + DIV + "KONTEKS INTEGRASI\n\nWorkstation AFTER-HOURS di-deploy sebagai dua situs statis. Output build workspace-os disajikan dari subdomain. Output build workspace-3d disajikan dari domain utama. File spesifikasi build mendefinisikan langkah-langkah pipeline CI/CD." + DIV + "TRADEOFF\n\nDeployment statis adalah opsi yang paling sederhana dan hemat biaya. Kendalanya adalah semua logika harus dieksekusi di browser. Deployment berbasis container mendukung beban kerja runtime apa pun tetapi memperkenalkan kompleksitas operasional. Pipeline CI/CD menambahkan penundaan wajib antara commit kode dan deployment produksi."
        },
        related: ["after-hours://knowledge/git", "after-hours://knowledge/nodejs", "after-hours://knowledge/rest-api"]
    }
];
